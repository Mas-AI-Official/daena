# Daena Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env files with real secrets!

# ===========================================
# CORE SETTINGS
# ===========================================
APP_NAME=Daena
APP_ENV=development
DEBUG=1
LOG_LEVEL=INFO

# ===========================================
# AUTHENTICATION (CRITICAL)
# ===========================================
# Set to 0 in production to REQUIRE authentication
DISABLE_AUTH=1

# JWT secret for token signing (generate a strong random value)
JWT_SECRET=CHANGE_ME_TO_RANDOM_64_CHAR_STRING

# Execution layer token (required for tool execution)
EXECUTION_TOKEN=CHANGE_ME_TO_SECURE_TOKEN

# ===========================================
# LLM PROVIDERS (choose one or more)
# ===========================================
# Enable cloud LLM (0 = local-first with Ollama)
ENABLE_CLOUD_LLM=0

# OpenAI (optional)
OPENAI_API_KEY=sk-your-openai-key-here

# Azure OpenAI (optional)
AZURE_OPENAI_API_KEY=your-azure-key-here
AZURE_OPENAI_API_BASE=https://your-resource.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_ID=your-deployment-id

# Google Gemini (optional)
GOOGLE_API_KEY=your-google-api-key

# Anthropic Claude (optional)
ANTHROPIC_API_KEY=your-anthropic-key

# ===========================================
# LOCAL MODELS (Ollama)
# ===========================================
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODELS=mistral:latest,codellama:latest,llama3:latest

# ===========================================
# MODEL STORAGE PATHS
# ===========================================
# Centralized model storage (recommended)
MODELS_ROOT=D:/Ideas/MODELS_ROOT

# HuggingFace cache location
HF_HOME=D:/Ideas/MODELS_ROOT/huggingface

# Brain/memory storage (separate from model weights)
BRAIN_ROOT=D:/Ideas/MODELS_ROOT/brain_storage

# ===========================================
# DATABASE
# ===========================================
DATABASE_URL=sqlite:///./daena.db

# ===========================================
# SECURITY
# ===========================================
# CORS origins (restrict in production)
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Workspace allowlist (comma-separated paths)
WORKSPACE_ALLOWLIST=D:/Ideas/Daena_old_upgrade_20251213,D:/Ideas/contracts

# ===========================================
# FEATURES
# ===========================================
# Enable execution layer (tool running)
ENABLE_EXECUTION_LAYER=1

# Enable browser automation
ENABLE_BROWSER_TOOLS=0

# Enable DeFi/Web3 security tools
ENABLE_DEFI_TOOLS=0

# Enable voice interface
ENABLE_VOICE=0

# Autonomous mode (agents can act without approval)
AUTONOMOUS_MODE=0

# ===========================================
# DAENABOT "HANDS" (OpenClaw Gateway)
# ===========================================
# Display name in UI (e.g. "DaenaBot" or "DanBot"). Control Panel tab and tools panel use this.
# DAENABOT_DISPLAY_NAME=DaenaBot

# Where the "hands" service (OpenClaw Gateway) runs. Bind to 127.0.0.1 only; do NOT expose to LAN.
DAENABOT_HANDS_URL=ws://127.0.0.1:18789/ws
# Token DaenaBot uses to authenticate to the gateway (paste from OpenClaw config/wizard)
DAENABOT_HANDS_TOKEN=put_the_gateway_token_here

# Legacy (optional): same values; kept for backward compatibility
# OPENCLAW_GATEWAY_URL=ws://127.0.0.1:18789/ws
# OPENCLAW_GATEWAY_TOKEN=put_the_same_token_here

# ===========================================
# EXTERNAL SERVICES (optional)
# ===========================================
# HuggingFace token (for model downloads)
# HUGGINGFACE_TOKEN=hf_your_token_here

# Discord webhook (for notifications)
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# Telegram bot (for notifications)
# TELEGRAM_BOT_TOKEN=your_bot_token
# TELEGRAM_CHAT_ID=your_chat_id

# ===========================================
# DEVELOPMENT
# ===========================================
# Enable dev tools endpoint
DAENA_DEV_MODE=0

# Hot reload
RELOAD=1
