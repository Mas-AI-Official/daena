# Audio/Voice/GenAI environment requirements
# Use with venv_daena_audio_py310 only!
google-genai>=1.19.0
anyio>=4.8.0,<5.0.0
httpx>=0.28.1,<1.0.0
websockets>=13.0.0,<15.1.0
# Voice cloning (ElevenLabs via aiohttp)
aiohttp>=3.9.0,<4.0.0
# Speech recognition
SpeechRecognition>=3.10.0
# Audio I/O (may require manual installation on Windows)
# Note: pyaudio may require Visual C++ Build Tools
# Alternative: pip install pipwin && pipwin install pyaudio
# Or download wheel from: https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# Wake word detection (requires Picovoice access key)
pvporcupine>=3.0.0
# Add other audio/voice dependencies below as needed 