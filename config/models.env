# Daena Model Configuration
# This file specifies which models to use for training and inference

# Default model for inference (if trained model not available)
DEFAULT_LOCAL_MODEL=qwen2.5:14b-instruct

# Fallback model (smaller, faster)
FALLBACK_LOCAL_MODEL=qwen2.5:7b-instruct

# Trained Daena brain model (will be created after training)
TRAINED_DAENA_MODEL=daena-brain

# Ollama configuration
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_TIMEOUT=120

# Model storage paths
MODELS_DIR=models
TRAINED_MODELS_DIR=models/trained
LOCAL_MODELS_DIR=models/local
